# lava-google

Autogenerated typed `(deflava-resource …)` shapes for every google
terraform-provider resource. Mirrors pangea-google.

## Regenerate

```bash
tofu providers schema -json > schema.json
lava-forge generate --schema schema.json --out resources
```
